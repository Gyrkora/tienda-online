import tow  from '../../tow.PNG';

export const Header = () => {
  return (
    <div>

    <header className="App-header">
        <img src={tow} className="App-logo" alt="logo" />
      </header> 
    </div>
  )
}

